# First run — saves model as default
cagent qwen2.5-coder:7b-instruct-q4_K_M --low-vram

# Subsequent runs — uses saved default
cagent

# Switch model mid-run
/model qwen2.5-coder:7b-instruct-q4_K_M

# Show / change GPU layers
/gpu-layers
/gpu-layers 20

# Shell commands run directly (no LLM)
ls
cat file.py
git status

# Shell passthrough with full control
!python3 script.py

# Navigate directories
cd /path/to/project
/pwd

# Context management
/compact
/tokens
/reset

# Exit
/bye   (or Ctrl+C / Ctrl+D)
