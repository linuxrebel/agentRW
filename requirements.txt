openai>=1.0.0

# Optional: Alt+Enter multi-line input. Falls back to input() if absent.
prompt_toolkit>=3.0.0

# Windows only: ANSI colors on consoles that lack native VT support.
colorama>=0.4.6; sys_platform == "win32"

# Plugin dependencies are NOT listed here, and no plugin ships with the agent.
# A plugin lives in its own repo, declares what it needs in its own install.md,
# gates its own registration, and stays dormant with a clear message when a
# requirement is absent — see PLUGINS.md.
#
# That matters because agentRW is not a Python-only tool. Someone using it for
# Go, Rust or shell should not be installing Python linters to run the harness,
# and now installs none of them: the above is the entire dependency list.
